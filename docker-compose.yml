services:
  geoapi:
    build: geoapi
    ports:
      - "5000:5000"
      - "9090:9090"
      - "9191:9191"
    environment:
      ENV: PROD
    env_file:
      - key.env
    volumes:
      - ./geoapi:/app
    links:
      - mongo

  mongo:
    image: mongo
